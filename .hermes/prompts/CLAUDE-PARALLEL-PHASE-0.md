# Moi (Claude) — Tâches en parallèle Phase 0\n\n**Pendant que Gemma génère boilerplate (15-20 min), je prépare :**\n\n---\n\n## 1. API Client Layer (`src/lib/api.ts`)\n\n```typescript\n// src/lib/api.ts\nimport type { \n  LoginResponse, \n  RegisterData, \n  RegisterResponse,\n  CalculateBody,\n  SynthesisResponse,\n  UserProfile\n} from './types';\n\nclass ApiError extends Error {\n  constructor(public status: number, message: string) {\n    super(message);\n    this.name = 'ApiError';\n  }\n}\n\nconst BASE = (() => {\n  if (typeof window === 'undefined') return '';\n  const isCapacitor = !!(window as any).Capacitor?.isNative;\n  return isCapacitor\n    ? 'https://gochara-api-732214018947.europe-west9.run.app'\n    : (import.meta.env.PUBLIC_API_URL || '');\n})();\n\nasync function request<T>(path: string, options?: RequestInit): Promise<T> {\n  const token = typeof localStorage !== 'undefined' ? localStorage.getItem('access_token') : null;\n  \n  const res = await fetch(`${BASE}${path}`, {\n    headers: {\n      'Content-Type': 'application/json',\n      ...(token ? { 'Authorization': `Bearer ${token}` } : {}),\n      ...options?.headers\n    },\n    credentials: 'include',\n    ...options,\n  });\n\n  if (res.status === 401) {\n    const refreshed = await refreshAccessToken();\n    if (refreshed) return request(path, options);\n    throw new ApiError(401, 'Unauthorized');\n  }\n\n  if (!res.ok) {\n    const msg = await res.text();\n    throw new ApiError(res.status, msg || 'Request failed');\n  }\n\n  return res.json();\n}\n\nasync function streamingRequest(path: string, body: object) {\n  const token = typeof localStorage !== 'undefined' ? localStorage.getItem('access_token') : null;\n  \n  const res = await fetch(`${BASE}${path}`, {\n    method: 'POST',\n    headers: {\n      'Content-Type': 'application/json',\n      ...(token ? { 'Authorization': `Bearer ${token}` } : {}),\n    },\n    credentials: 'include',\n    body: JSON.stringify(body)\n  });\n\n  if (!res.ok) throw new ApiError(res.status, await res.text());\n  return res.body?.getReader();\n}\n\nexport const api = {\n  // Auth\n  login: (pseudo: string) => \n    request<LoginResponse>('/auth/login', {\n      method: 'POST',\n      body: JSON.stringify({ pseudo })\n    }),\n\n  register: (data: RegisterData) =>\n    request<RegisterResponse>('/auth/register', {\n      method: 'POST',\n      body: JSON.stringify(data)\n    }),\n\n  // Calculations\n  calculate: (body: CalculateBody) =>\n    streamingRequest('/calculate', body),\n\n  // Profile\n  profile: () => request<UserProfile>('/api/profile'),\n\n  // Stripe\n  stripeCheckout: (product_type: string) =>\n    request<{ ok: boolean; beta: boolean; checkout_url?: string }>('/stripe/checkout', {\n      method: 'POST',\n      body: JSON.stringify({ product_type })\n    }),\n\n  // Geocode\n  geocode: (q: string) =>\n    request<any[]>('/geocode', {\n      method: 'GET'\n    }).then(() => fetch(`/geocode?q=${encodeURIComponent(q)}`).then(r => r.json())),\n};\n\nasync function refreshAccessToken(): Promise<boolean> {\n  if (typeof localStorage === 'undefined') return false;\n  const refresh = localStorage.getItem('refresh_token');\n  if (!refresh) return false;\n  \n  try {\n    const res = await fetch(`${BASE}/auth/refresh`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ refresh_token: refresh })\n    });\n    if (res.ok) {\n      const { access_token } = await res.json();\n      localStorage.setItem('access_token', access_token);\n      return true;\n    }\n  } catch {}\n  return false;\n}\n\nexport { ApiError };\n```\n\n---\n\n## 2. Auth Helper (`src/lib/auth.ts`)\n\n```typescript\n// src/lib/auth.ts\nimport { api } from './api';\n\nexport async function login(pseudo: string) {\n  const resp = await api.login(pseudo);\n  if (!resp.ok) throw new Error(resp.error || 'Login failed');\n  \n  localStorage.setItem('access_token', resp.access_token);\n  localStorage.setItem('refresh_token', resp.refresh_token);\n  return resp;\n}\n\nexport async function register(data: any) {\n  const resp = await api.register(data);\n  if (!resp.ok) throw new Error(resp.error || 'Register failed');\n  \n  localStorage.setItem('access_token', resp.access_token);\n  localStorage.setItem('refresh_token', resp.refresh_token);\n  return resp;\n}\n\nexport function logout() {\n  localStorage.removeItem('access_token');\n  localStorage.removeItem('refresh_token');\n  window.location.href = '/';\n}\n\nexport function getToken(): string | null {\n  return typeof localStorage !== 'undefined' ? localStorage.getItem('access_token') : null;\n}\n\nexport async function isAuthenticated(): Promise<boolean> {\n  try {\n    await api.profile();\n    return true;\n  } catch {\n    return false;\n  }\n}\n```\n\n---\n\n## 3. Capacitor Bridge (`src/lib/capacitor-bridge.ts`)\n\n```typescript\n// src/lib/capacitor-bridge.ts\nexport const capacitorBridge = {\n  isNative: (): boolean => {\n    if (typeof window === 'undefined') return false;\n    return !!(window as any).Capacitor?.isNative;\n  },\n\n  callPlugin: async <T = any>(\n    name: string,\n    method: string,\n    args: any\n  ): Promise<T | null> => {\n    if (!capacitorBridge.isNative()) return null;\n    \n    const plugins = (window as any).Capacitor?.Plugins;\n    if (!plugins || !plugins[name]) {\n      throw new Error(`Plugin ${name} not found`);\n    }\n    \n    return plugins[name][method](args);\n  },\n\n  // Convenience methods for common plugins\n  gemma: {\n    synthesize: (args: any) =>\n      capacitorBridge.callPlugin('GemmaSynthesis', 'synthesize', args),\n  },\n\n  statusBar: {\n    setStyle: (style: 'DARK' | 'LIGHT') =>\n      capacitorBridge.callPlugin('StatusBar', 'setStyle', { style }),\n  },\n};\n```\n\n---\n\n## 4. Flask Refactor (JWT endpoints)\n\n```python\n# app.py — additions\n\nimport jwt\nfrom datetime import datetime, timedelta\nfrom functools import wraps\nfrom flask_cors import CORS\nimport os\n\n# Config\nJWT_SECRET = os.getenv('JWT_SECRET', 'dev-secret-change-in-prod')\nJWT_ALGORITHM = 'HS256'\nJWT_ACCESS_EXPIRY = timedelta(hours=24)\nJWT_REFRESH_EXPIRY = timedelta(days=7)\n\n# CORS\nCORS(\n    app,\n    origins=[\n        'https://karmicgochara.app',\n        'https://app.karmicgochara.app',\n        'capacitor://localhost',\n        'http://localhost:3000'  # dev\n    ],\n    supports_credentials=True,\n    allow_headers=['Authorization', 'Content-Type']\n)\n\n# JWT helpers\ndef create_access_token(user_id: str) -> str:\n    payload = {\n        'user_id': user_id,\n        'exp': datetime.utcnow() + JWT_ACCESS_EXPIRY,\n        'iat': datetime.utcnow()\n    }\n    return jwt.encode(payload, JWT_SECRET, algorithm=JWT_ALGORITHM)\n\ndef create_refresh_token(user_id: str) -> str:\n    payload = {\n        'user_id': user_id,\n        'exp': datetime.utcnow() + JWT_REFRESH_EXPIRY,\n        'iat': datetime.utcnow()\n    }\n    return jwt.encode(payload, JWT_SECRET, algorithm=JWT_ALGORITHM)\n\ndef token_required(f):\n    @wraps(f)\n    def decorated(*args, **kwargs):\n        auth = request.headers.get('Authorization', '')\n        if not auth.startswith('Bearer '):\n            return {'error': 'Missing authorization header'}, 401\n        \n        token = auth.split(' ', 1)[1]\n        try:\n            payload = jwt.decode(token, JWT_SECRET, algorithms=[JWT_ALGORITHM])\n            request.user_id = payload['user_id']\n        except jwt.ExpiredSignatureError:\n            return {'error': 'Token expired'}, 401\n        except jwt.InvalidTokenError:\n            return {'error': 'Invalid token'}, 401\n        \n        return f(*args, **kwargs)\n    return decorated\n\n# Routes\n@app.route('/auth/login', methods=['POST'])\ndef auth_login():\n    data = request.json or {}\n    pseudo = data.get('pseudo', '').strip()\n    \n    if not pseudo:\n        return {'ok': False, 'error': 'Pseudo required'}, 400\n    \n    user = User.query.filter_by(pseudo=pseudo).first()\n    if not user:\n        return {'ok': False, 'error': 'User not found'}, 401\n    \n    access = create_access_token(user.id)\n    refresh = create_refresh_token(user.id)\n    \n    return {\n        'ok': True,\n        'access_token': access,\n        'refresh_token': refresh,\n        'pseudo': user.pseudo\n    }\n\n@app.route('/auth/refresh', methods=['POST'])\ndef auth_refresh():\n    data = request.json or {}\n    refresh_token = data.get('refresh_token', '')\n    \n    if not refresh_token:\n        return {'ok': False, 'error': 'Refresh token required'}, 400\n    \n    try:\n        payload = jwt.decode(refresh_token, JWT_SECRET, algorithms=[JWT_ALGORITHM])\n        user_id = payload['user_id']\n        access = create_access_token(user_id)\n        return {'ok': True, 'access_token': access}\n    except jwt.InvalidTokenError:\n        return {'ok': False, 'error': 'Invalid refresh token'}, 401\n\n@app.route('/api/profile')\n@token_required\ndef api_profile():\n    user = User.query.get(request.user_id)\n    if not user:\n        return {'error': 'User not found'}, 404\n    return {'user': user.to_dict()}\n\n# Apply @token_required to protected endpoints:\n# @app.route('/calculate', methods=['POST'])\n# @token_required\n# def calculate():\n#   user = User.query.get(request.user_id)\n#   ...\n```\n\n---\n\n## 5. Android Beta Now (provisioning)\n\n### Prepare APK release build\n\n```bash\ncd /Users/jero87/karmic.gochara\n\n# 1. Build web static (Flask current state)\n# (skip Astro — using Flask web for now)\n\n# 2. Sync Capacitor with current www/\nnpx cap sync android\n\n# 3. Build Android release\ncd android\n./gradlew bundleRelease\n# Output: android/app/build/outputs/bundle/release/app-release.aab\n```\n\n### Upload to Google Play Console\n\n1. Open Google Play Console → Karmic Gochara\n2. Testing → Internal testing\n3. Upload new release (app-release.aab)\n4. Add internal testers (email list)\n5. Release to internal testing\n\n**Testeurs can download from Play Store internal test link (email)**\n\n---\n\n## 📋 Checklist parallèle\n\n- [ ] `src/lib/api.ts` créé\n- [ ] `src/lib/auth.ts` créé\n- [ ] `src/lib/capacitor-bridge.ts` créé\n- [ ] Flask `/auth/login`, `/auth/refresh` endpoints définis\n- [ ] Flask `@token_required` middleware activé\n- [ ] CORS config mis à jour\n- [ ] Android bundle prêt pour testeurs\n\n---\n\n## ✅ Jour 4-5 : Fusion\n\n- Copier fichiers Gemma dans `astro/`\n- Intégrer JWT logic\n- Test `/auth/login` end-to-end\n- Phase 0.75 validation\n"