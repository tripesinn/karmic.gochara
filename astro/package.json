{
  "name": "karmic-gochara-astro",
  "version": "1.9.8",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:prod": "PUBLIC_FIREBASE_EMULATOR=false astro build",
    "build:emulator": "PUBLIC_FIREBASE_EMULATOR=true astro build",
    "preview": "astro preview",
    "sync:capacitor": "PUBLIC_FIREBASE_EMULATOR=false astro build && cd .. && npx cap sync",
    "sync:prod": "PUBLIC_FIREBASE_EMULATOR=false astro build && cd .. && npx cap sync",
    "sync:emulator": "PUBLIC_FIREBASE_EMULATOR=true astro build && cd .. && npx cap sync"

  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "@capacitor/core": "^8.4.1",
    "@firebase/app": "^0.15.0",
    "@firebase/auth": "^1.13.3",
    "@revenuecat/purchases-capacitor": "^13.2.0",
    "astro": "^5.0.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.5.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
