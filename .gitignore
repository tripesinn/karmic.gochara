# Swiss Ephemeris (Windows local uniquement)
*.dll
*.h

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# Propriété intellectuelle — doctrine karmique (hébergée via Render Secret Files)
doctrine.py
karmic_vault/

# Secrets & credentials — NE JAMAIS COMMITTER
credentials.json
*.json.key
service_account*.json

# Node / Capacitor
node_modules/

# Android (généré — committer uniquement les sources)
android/.gradle/
android/app/build/
android/build/
android/.idea/

# Gradle home cache (généré localement, jamais committer)
caches/
daemon/
native/
wrapper/

# Cloudflare Wrangler (état local, cache R2, DB miniflare)
.wrangler/

# Claude Code (settings locaux)
.claude/settings.local.json

# IDE
.vscode/settings.json

# Vault
vault/

# Android Signing Key
karmic-key
*.jks
*.keystore
.aider*

# Local scratch / large model files
scratch/
.env.local
**/env.local

# Logs and screenshots
*.log
*.png
window_dump.xml
logcat.txt

# Test tools and local scripts
create_test_user.py
insert_tester.py
scripts/moa.py
scripts/start_emulator.sh
**/.DS_Store
*.bak
.agents/skills/local-corrector/
astro/.env.local.example
